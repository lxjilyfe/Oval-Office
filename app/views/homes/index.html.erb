<h1>Listing homes</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Comment</th>
  </tr>

<% @homes.each do |home| %>
  <tr>
    <td><%=h home.title %></td>
    <td><%=h home.comment %></td>
    <td><%= link_to 'Show', home %></td>
    <td><%= link_to 'Edit', edit_home_path(home) %></td>
    <td><%= link_to 'Destroy', home, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New home', new_home_path %>